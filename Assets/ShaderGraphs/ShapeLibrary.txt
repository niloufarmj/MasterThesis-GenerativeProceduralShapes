a simple cartoon crown with a flat horizontal base band and a single continuous crown silhouette above it, where the top edge forms a zigzag shape with three rounded peaks and each peak has a small circular jewel on top, all parts connected into one shape with no separate spikes or floating geometry, and I can adjust the overall width and height, the height of the zigzag peaks, the roundness of the tips, the size of the jewels, and set separate colors for the base band, the crown body, and the jewels