# Simple shapes — single-component geometric primitives
# SIMILAR to shapes already in the RAG knowledge base (final-shape-list)
# Two active items for initial code test; remaining 8 are commented out.
# One prompt per line. Lines starting with # are ignored by the runner.

a filled circle in dark color
a five-pointed star with five equal pointed tips 

# a ring or annulus with a uniform border width
# an equilateral triangle with all three sides equal
# a rounded rectangle with uniformly curved corners
# a heart shape with two rounded lobes at the top meeting a pointed bottom tip
# a crescent moon formed by overlapping two circles of different sizes
# a cross or plus sign with four arms of equal length and width
# a diamond shape formed by rotating a square 45 degrees
# a simple inward spiral with two revolutions winding from the outside to the center
